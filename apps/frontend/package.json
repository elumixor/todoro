{
  "name": "frontend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --host --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "cap sync ios",
    "ios": "CAPACITOR_LIVE_RELOAD=true cap sync ios && cap open ios",
    "ios:prod": "VITE_API_URL=https://todoro-backend.vercel.app vite build && cap sync ios && cap open ios"
  },
  "dependencies": {
    "backend": "workspace:*",
    "@capacitor/app": "^8.0.1",
    "@capacitor/core": "^8",
    "@capacitor/ios": "^8",
    "lucide-svelte": "^1.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8",
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.20.4",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tailwindcss/vite": "^4.2.2",
    "svelte": "^5.25.6",
    "tailwindcss": "^4.1.3",
    "vite": "^6.2.5"
  }
}

{
  "framework": null,
  "installCommand": "cd ../.. && bun install",
  "buildCommand": "cd ../backend && bun run client:generate && cd ../frontend && bun run build",
  "outputDirectory": "build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
